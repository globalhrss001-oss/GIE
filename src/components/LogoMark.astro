---
import { company } from '../data/company';
import { asset } from '../utils/url';

interface Props {
  size?: 'xs' | 'sm' | 'md' | 'lg' | 'xl';
  onDark?: boolean;
  class?: string;
}

const { size = 'md', onDark = false, class: className = '' } = Astro.props;

const sizes = {
  xs: 'h-8 w-8 max-w-[32px]',
  sm: 'h-12 w-12 max-w-[48px]',
  md: 'h-16 w-16 max-w-[64px]',
  lg: 'h-24 w-24 max-w-[96px]',
  xl: 'h-28 w-28 max-w-[112px] sm:h-32 sm:w-32 sm:max-w-[128px]',
};
---

{
  onDark ? (
    <span class:list={['inline-block rounded-lg bg-white p-2.5 shadow-sm sm:p-3', className]}>
      <img
        src={asset('images/logo/gie-logo.png')}
        alt={company.logoAlt}
        class:list={['w-auto object-contain', sizes[size]]}
        width="180"
        height="80"
        loading="lazy"
      />
    </span>
  ) : (
    <img
      src={asset('images/logo/gie-logo.png')}
      alt={company.logoAlt}
      class:list={['w-auto object-contain', sizes[size], className]}
      width="180"
      height="80"
      loading="lazy"
    />
  )
}
